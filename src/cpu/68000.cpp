#include "68000.h"
#include <cstdint>
#include <stdio.h>

class
{
public:
	static void SET_CPU(MAIN* main)
	{
		MAIN* cpu = main;
		cpu->PROGRAM_COUNTER;
	}

	static void CPU_RESET(FUNC* RESET)
	{

	}
};
